#include "Ве_крест_крест.h"

внедрить хутор Русь;

династия Узел
{
public:
    Узел* следующий;
    целина значение;

    Узел(целина значение) : следующий(туда_не_знаю_куда), значение(значение) {}
};

династия Связный_Список
{
private:
    Узел* начало;
    Узел* конец;

public:
    Связный_Список() : начало(туда_не_знаю_куда), конец(туда_не_знаю_куда) {}

    бестолочь добавить(целина значение)
    {
        Узел* новый_узел = новь Узел(значение);

        коли (начало == туда_не_знаю_куда)
        {
            начало = новый_узел;
            конец = новый_узел;
        }
        отнюдь
        {
            конец->следующий = новый_узел;
            конец = новый_узел;
        }
    }

    бестолочь удалить(целина значение)
    {
        Узел* текущий = начало;
        Узел* предыдущий = туда_не_знаю_куда;

        покуда (текущий != туда_не_знаю_куда)
        {
            коли (текущий->значение == значение)
            {
                коли (предыдущий == туда_не_знаю_куда)
                {
                    начало = текущий->следующий;
                }
                отнюдь
                {
                    предыдущий->следующий = текущий->следующий;
                }

                коли (текущий == конец)
                {
                    конец = предыдущий;
                }

                казнь текущий;
            }

            предыдущий = текущий;
            текущий = текущий->следующий;
        }
    }

    бестолочь печать()
    {
        Узел* текущий = начало;
        покуда (текущий != туда_не_знаю_куда)
        {
            молвить << текущий->значение << " ";
            текущий = текущий->следующий;
        }

        молвить << прыг_скок;
    }
};

царь_батюшка_главный()
{
    вперёд_славяне;

    Связный_Список список;

    список.добавить(42);
    список.добавить(10);
    список.добавить(24);

    молвить << "Список после добавления элементов:" << прыг_скок;
    список.печать();

    список.удалить(10);

    молвить << "Список после удаления элемента 10:" << прыг_скок;
    список.печать();

    воздать 0;
}
